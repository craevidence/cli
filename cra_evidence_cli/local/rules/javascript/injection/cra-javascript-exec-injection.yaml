rules:
  - id: cra-javascript-exec-injection
    message: >
      child_process exec() or execSync() called with a command string built by
      concatenation. If any part of the string comes from untrusted input this
      allows OS command injection. Use execFile() or spawn() with an argument
      array instead.
    severity: ERROR
    metadata:
      tier: experimental
      cwe:
        - "CWE-78: Improper Neutralization of Special Elements used in an OS Command"
      owasp:
        - "A03:2021 - Injection"
      references:
        - https://cwe.mitre.org/data/definitions/78.html
        - https://owasp.org/Top10/A03_2021-Injection/
      category: security
      technology:
        - javascript
      confidence: MEDIUM
      scope: >
        Reports process input reaching the command string of child_process
        exec or execSync. The binding is recognised when it comes from a
        require call, a destructured require with or without renaming, an ESM
        import, or a TypeScript import-equals, in the same file.
      limitations: >
        Analysis is single file, so a binding re-exported through a local
        helper module is not followed. A binding installed on globalThis and a
        module obtained through a dynamic import expression are not matched,
        both confirmed by probe. A call wrapped by promisify is not matched. A
        command whose only interpolation is a conditional between two string
        literals is constant in every branch, and reporting it is a false
        positive: one such call was found in production code on a scan of three
        real Node projects. A carve-out for that shape was written and then
        rejected on measurement. Excluding a template that interpolates a
        conditional between two string literals also silences a template that
        interpolates such a conditional alongside an attacker controlled value,
        in either order, which trades one false positive for two false
        negatives. The finding therefore stands.
      license: MIT
      author: CRA Evidence
    languages: [javascript, typescript]
    pattern-either:
      - patterns:
          - pattern-either:
              - pattern: $CP.exec($A + $B, ...)
              - pattern: $CP.execSync($A + $B, ...)
              - pattern: $CP.exec(`...${$VALUE}...`, ...)
              - pattern: $CP.execSync(`...${$VALUE}...`, ...)
          - pattern-either:
              - pattern-inside: |
                  const $CP = require("child_process");
                  ...
              - pattern-inside: |
                  var $CP = require("child_process");
                  ...
              - pattern-inside: |
                  let $CP = require("child_process");
                  ...
              - pattern-inside: |
                  const $CP = require("node:child_process");
                  ...
              - pattern-inside: |
                  var $CP = require("node:child_process");
                  ...
              - pattern-inside: |
                  let $CP = require("node:child_process");
                  ...
              - pattern-inside: |
                  import $CP from "child_process";
                  ...
              - pattern-inside: |
                  import $CP from "node:child_process";
                  ...
              - pattern-inside: |
                  import * as $CP from "child_process";
                  ...
              - pattern-inside: |
                  import * as $CP from "node:child_process";
                  ...
      - pattern: require('child_process').exec($A + $B, ...)
      - pattern: require('child_process').execSync($A + $B, ...)
      - pattern: require('child_process').exec(`...${$VALUE}...`, ...)
      - pattern: require('child_process').execSync(`...${$VALUE}...`, ...)
      - pattern: require('node:child_process').exec($A + $B, ...)
      - pattern: require('node:child_process').execSync($A + $B, ...)
      - pattern: require('node:child_process').exec(`...${$VALUE}...`, ...)
      - pattern: require('node:child_process').execSync(`...${$VALUE}...`, ...)
      - patterns:
          - pattern-either:
              - pattern: exec($A + $B, ...)
              - pattern: execSync($A + $B, ...)
              - pattern: exec(`...${$VALUE}...`, ...)
              - pattern: execSync(`...${$VALUE}...`, ...)
          - pattern-either:
              - pattern-inside: |
                  import { exec } from "child_process";
                  ...
              - pattern-inside: |
                  import { exec } from "node:child_process";
                  ...
              - pattern-inside: |
                  import { execSync } from "child_process";
                  ...
              - pattern-inside: |
                  import { execSync } from "node:child_process";
                  ...
              - pattern-inside: |
                  const { exec, execSync } = require("child_process");
                  ...
              - pattern-inside: |
                  var { exec, execSync } = require("child_process");
                  ...
              - pattern-inside: |
                  let { exec, execSync } = require("child_process");
                  ...
              - pattern-inside: |
                  const { exec, execSync } = require("node:child_process");
                  ...
              - pattern-inside: |
                  var { exec, execSync } = require("node:child_process");
                  ...
              - pattern-inside: |
                  let { exec, execSync } = require("node:child_process");
                  ...
              - pattern-inside: |
                  import { exec, execSync } from "child_process";
                  ...
              - pattern-inside: |
                  import { exec, execSync } from "node:child_process";
                  ...
      - patterns:
          - pattern-either:
              - pattern: $EXEC($A + $B, ...)
              - pattern: $EXEC(`...${$VALUE}...`, ...)
          - pattern-either:
              - pattern-inside: |
                  $EXEC = require("child_process").exec;
                  ...
              - pattern-inside: |
                  $EXEC = require("node:child_process").exec;
                  ...
              - pattern-inside: |
                  $EXEC = require("child_process").execSync;
                  ...
              - pattern-inside: |
                  $EXEC = require("node:child_process").execSync;
                  ...
              - pattern-inside: |
                  const { exec: $EXEC } = require("child_process");
                  ...
              - pattern-inside: |
                  var { exec: $EXEC } = require("child_process");
                  ...
              - pattern-inside: |
                  let { exec: $EXEC } = require("child_process");
                  ...
              - pattern-inside: |
                  const { exec: $EXEC } = require("node:child_process");
                  ...
              - pattern-inside: |
                  var { exec: $EXEC } = require("node:child_process");
                  ...
              - pattern-inside: |
                  let { exec: $EXEC } = require("node:child_process");
                  ...
              - pattern-inside: |
                  const { execSync: $EXEC } = require("child_process");
                  ...
              - pattern-inside: |
                  var { execSync: $EXEC } = require("child_process");
                  ...
              - pattern-inside: |
                  let { execSync: $EXEC } = require("child_process");
                  ...
              - pattern-inside: |
                  const { execSync: $EXEC } = require("node:child_process");
                  ...
              - pattern-inside: |
                  var { execSync: $EXEC } = require("node:child_process");
                  ...
              - pattern-inside: |
                  let { execSync: $EXEC } = require("node:child_process");
                  ...
              - pattern-inside: |
                  import { exec as $EXEC } from "child_process";
                  ...
              - pattern-inside: |
                  import { exec as $EXEC } from "node:child_process";
                  ...
              - pattern-inside: |
                  import { execSync as $EXEC } from "child_process";
                  ...
              - pattern-inside: |
                  import { execSync as $EXEC } from "node:child_process";
                  ...
