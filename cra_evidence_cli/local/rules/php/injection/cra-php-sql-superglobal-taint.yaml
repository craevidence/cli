rules:
  - id: cra-php-sql-superglobal-taint
    message: >
      HTTP request data reaches a SQL query as query text. Use a prepared
      statement with placeholders and bind each untrusted value.
    severity: ERROR
    metadata:
      tier: experimental
      cwe: ["CWE-89: Improper Neutralization of Special Elements used in an SQL Command"]
      owasp: ["A03:2021 - Injection"]
      references:
        - https://cwe.mitre.org/data/definitions/89.html
        - https://www.php.net/manual/en/pdo.prepare.php
      category: security
      technology: [php, pdo, mysqli]
      confidence: HIGH
      license: MIT
      author: CRA Evidence
    languages: [php]
    mode: taint
    pattern-sources:
      - pattern-either:
          - pattern: $_GET
          - pattern: $_POST
          - pattern: $_REQUEST
          - pattern: $_COOKIE
          - patterns:
              - pattern: $_SERVER[$KEY]
              - metavariable-regex:
                  metavariable: $KEY
                  regex: ^["'](HTTP_[A-Z_]+|PATH_INFO|QUERY_STRING|REQUEST_URI|PHP_AUTH_USER|PHP_AUTH_PW)["']$
    pattern-sanitizers:
      - pattern-either:
          - pattern: intval(...)
          - pattern: filter_var(..., FILTER_VALIDATE_INT, ...)
    pattern-sinks:
      - pattern-either:
          - patterns:
              - pattern: $DB->query($QUERY)
              - focus-metavariable: $QUERY
          - patterns:
              - pattern: $DB->exec($QUERY)
              - focus-metavariable: $QUERY
          - patterns:
              - pattern: $DB->prepare($QUERY)
              - focus-metavariable: $QUERY
          - patterns:
              - pattern: mysqli_query($DB, $QUERY)
              - focus-metavariable: $QUERY
          - patterns:
              - pattern: mysqli_prepare($DB, $QUERY)
              - focus-metavariable: $QUERY
