name: CI - Build, Test, SBOM, Publish

on:
  push:
    branches: [main]
    paths-ignore:
      - "*.md"
      - "LICENSE"
  pull_request:
    branches: [main]
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  # Job 1: Run tests
  # ---------------------------------------------------------------------------
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest tests/ -v --tb=short

  # ---------------------------------------------------------------------------
  # Job 2: Rule pack structural gate + engine gate + dist content guard
  # ---------------------------------------------------------------------------
  rulepack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install build

      - name: Read pinned opengrep version
        id: opengrep-version
        run: |
          VERSION=$(python -c "from cra_evidence_cli.local.rules_pack import TESTED_OPENGREP_VERSION; print(TESTED_OPENGREP_VERSION)")
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Install opengrep ${{ steps.opengrep-version.outputs.version }}
        run: |
          VERSION="${{ steps.opengrep-version.outputs.version }}"
          ARCH="$(uname -m)"
          # Map uname arch to the opengrep release asset name
          case "$ARCH" in
            x86_64)  OG_ASSET="opengrep_manylinux_x86" ;;
            aarch64) OG_ASSET="opengrep_manylinux_aarch64" ;;
            *)        echo "Unsupported arch: $ARCH" >&2; exit 1 ;;
          esac
          URL="https://github.com/opengrep/opengrep/releases/download/v${VERSION}/${OG_ASSET}"
          curl -fsSL "$URL" -o /usr/local/bin/opengrep
          chmod +x /usr/local/bin/opengrep
          opengrep --version

      - name: Structural gate (pyyaml)
        run: pytest tests/test_rulepack.py -v --tb=short

      - name: Engine gate (opengrep per-rule)
        run: bash scripts/rulepack_gate.sh

      - name: Dist content guard
        run: python scripts/check_dist.py

  # ---------------------------------------------------------------------------
  # Job 3: Build Docker image, generate SBOM, upload to CRA Evidence
  # ---------------------------------------------------------------------------
  build-and-sbom:
    runs-on: ubuntu-latest
    needs: [test, rulepack]
    permissions:
      contents: read
      packages: write
      id-token: write  # keyless cosign signing via Sigstore OIDC
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Get version from pyproject.toml
        id: version
        run: |
          VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c  # v4.2.0

      # Login to the DHI registry to pull pinned base images. When DHI_REGISTRY_USERNAME
      # or DHI_REGISTRY_TOKEN is absent (e.g., forks or PRs without repository secrets),
      # the step fails gracefully and the local build falls back to public Python base
      # images via --build-arg (see the build step below).
      - name: Login to DHI registry
        id: dhi-login
        continue-on-error: true
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0  # v4.4.0
        with:
          registry: dhi.io
          username: ${{ secrets.DHI_REGISTRY_USERNAME }}
          password: ${{ secrets.DHI_REGISTRY_TOKEN }}

      - name: Build CLI Docker image
        run: |
          if [ "${{ steps.dhi-login.outcome }}" = "success" ]; then
            docker build -t craevidence:${{ steps.version.outputs.version }} .
          else
            # DHI registry unavailable; use public Python base images as fallback
            docker build \
              --build-arg BASE_IMAGE_BUILDER=python:3.14 \
              --build-arg BASE_IMAGE=python:3.14-slim \
              -t craevidence:${{ steps.version.outputs.version }} .
          fi

      # Security gate: fail the build if HIGH/CRITICAL vulnerabilities found in the Docker image.
      # Only fixed vulnerabilities are flagged to avoid false positives (e.g. glibc in base image).
      # Suppressions live in .github/grype.yaml, loaded via GRYPE_CONFIG.
      - name: Scan image for vulnerabilities (Grype)
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2  # v7.4.0
        env:
          GRYPE_CONFIG: .github/grype.yaml
        with:
          image: craevidence:${{ steps.version.outputs.version }}
          severity-cutoff: high
          fail-build: true
          only-fixed: true

      - name: Install Syft
        uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610  # v0

      - name: Generate SBOM (CycloneDX)
        run: |
          syft craevidence:${{ steps.version.outputs.version }} \
            -o cyclonedx-json=sbom.cdx.json

      - name: Upload SBOM to CRA Evidence
        if: github.event_name == 'release'
        continue-on-error: true
        env:
          CRA_EVIDENCE_API_KEY: ${{ secrets.CRA_EVIDENCE_API_KEY }}
          CRA_EVIDENCE_URL: https://api.craevidence.com
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/workspace \
            -e CRA_EVIDENCE_API_KEY \
            -e CRA_EVIDENCE_URL \
            craevidence:${{ steps.version.outputs.version }} \
            upload-sbom \
              --product cra-evidence-cli \
              --component cli \
              --version ${{ steps.version.outputs.version }} \
              --file /workspace/sbom.cdx.json \
              --no-create-product \
              --create-version \
              --scan

      - name: Upload SBOM as artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom
          path: sbom.cdx.json

      # Registry logins must precede multi-arch push steps so buildx can authenticate
      # when pushing layers to each registry.
      - name: Login to Docker Hub
        if: github.event_name == 'release'
        id: dockerhub-login
        continue-on-error: true
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0  # v4.4.0
        with:
          username: craevidence
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GHCR
        if: github.event_name == 'release'
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0  # v4.4.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Quay.io
        if: github.event_name == 'release'
        id: quay-login
        continue-on-error: true
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0  # v4.4.0
        with:
          registry: quay.io
          username: craevidence+all
          password: ${{ secrets.QUAY_TOKEN }}

      # Multi-arch release builds: each registry receives a linux/amd64 + linux/arm64
      # manifest list. The manifest index digest is captured from the GHCR step (always
      # available via GITHUB_TOKEN) and reused for all signing steps; the digest is
      # content-addressed, so it is identical across registries for identical content.
      - name: Build and push multi-arch image to GHCR
        if: github.event_name == 'release'
        id: build-ghcr
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/craevidence:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/craevidence:latest
          build-args: |
            ${{ steps.dhi-login.outcome != 'success' && 'BASE_IMAGE_BUILDER=python:3.14' || '' }}
            ${{ steps.dhi-login.outcome != 'success' && 'BASE_IMAGE=python:3.14-slim' || '' }}

      - name: Build and push multi-arch image to Docker Hub
        if: github.event_name == 'release' && steps.dockerhub-login.outcome == 'success'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ vars.DOCKERHUB_IMAGE }}:${{ steps.version.outputs.version }}
            ${{ vars.DOCKERHUB_IMAGE }}:latest
          build-args: |
            ${{ steps.dhi-login.outcome != 'success' && 'BASE_IMAGE_BUILDER=python:3.14' || '' }}
            ${{ steps.dhi-login.outcome != 'success' && 'BASE_IMAGE=python:3.14-slim' || '' }}

      - name: Build and push multi-arch image to Quay.io
        if: github.event_name == 'release' && steps.quay-login.outcome == 'success'
        continue-on-error: true
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            quay.io/craevidence/cli:${{ steps.version.outputs.version }}
            quay.io/craevidence/cli:latest
          build-args: |
            ${{ steps.dhi-login.outcome != 'success' && 'BASE_IMAGE_BUILDER=python:3.14' || '' }}
            ${{ steps.dhi-login.outcome != 'success' && 'BASE_IMAGE=python:3.14-slim' || '' }}

      # Keyless signing of the pushed manifest lists (Sigstore OIDC, no long-lived keys).
      # All three signing steps reference the manifest index digest from the GHCR build;
      # the digest is identical across registries for identical image content.
      - name: Install cosign
        if: github.event_name == 'release'
        uses: sigstore/cosign-installer@dc72c7d5c4d10cd6bcb8cf6e3fd625a9e5e537da  # v3.7.0

      - name: Sign GHCR image
        if: github.event_name == 'release'
        env:
          COSIGN_YES: "true"
        run: |
          cosign sign --yes \
            "ghcr.io/${{ github.repository_owner }}/craevidence@${{ steps.build-ghcr.outputs.digest }}"

      - name: Sign Docker Hub image
        if: github.event_name == 'release' && steps.dockerhub-login.outcome == 'success'
        env:
          COSIGN_YES: "true"
        run: |
          cosign sign --yes "${{ vars.DOCKERHUB_IMAGE }}@${{ steps.build-ghcr.outputs.digest }}"

      - name: Sign Quay.io image
        if: github.event_name == 'release' && steps.quay-login.outcome == 'success'
        continue-on-error: true
        env:
          COSIGN_YES: "true"
        run: |
          cosign sign --yes "quay.io/craevidence/cli@${{ steps.build-ghcr.outputs.digest }}"

  # ---------------------------------------------------------------------------
  # Job 3: Publish to PyPI (on release)
  # ---------------------------------------------------------------------------
  # Publishes the sdist and wheel to PyPI via Trusted Publishing (OIDC, no API
  # token) and attaches PEP 740 Sigstore attestations to each artifact.
  #
  # Prerequisites (one-time, configured outside this file):
  #   - Register the `craevidence` project on PyPI.
  #   - Add this repository + the `pypi` environment as a Trusted Publisher in
  #     the PyPI project settings.
  # ---------------------------------------------------------------------------
  publish-pypi:
    runs-on: ubuntu-latest
    needs: [test, rulepack, build-and-sbom]
    if: github.event_name == 'release'
    environment: pypi
    permissions:
      contents: write  # upload signed artifacts to the GitHub release
      id-token: write  # OIDC for Trusted Publishing, PEP 740 attestations, Sigstore signing
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      # Refuse to publish artifacts whose metadata version does not match the
      # release tag. PyPI uploads are immutable, so a v3.6.0 release must carry
      # exactly version 3.6.0.
      - name: Verify release tag matches package version
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          if [ "${RELEASE_TAG}" != "v${VERSION}" ]; then
            echo "::error::Release tag '${RELEASE_TAG}' does not match package version 'v${VERSION}'"
            exit 1
          fi
          echo "Release tag '${RELEASE_TAG}' matches package version 'v${VERSION}'."

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: "3.14"

      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build

      # Sign before publishing so a signing failure stops the job while the
      # release is still fully reversible; the PyPI upload below is immutable.
      # The same artifacts are published to PyPI and attached to the GitHub
      # release with their Sigstore bundles, so the release page carries
      # verifiable signatures for every distribution.
      - name: Sign artifacts with Sigstore
        run: |
          python -m pip install sigstore
          python -m sigstore sign dist/*.tar.gz dist/*.whl
          # Keep dist/ distribution-only for the PyPI upload step.
          mkdir -p sigstore-bundles
          mv dist/*.sigstore.json sigstore-bundles/

      # skip-existing makes a rerun after a post-publish failure idempotent;
      # the tag-check step above already guards against version mixups.
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@ed0c53931b1dc9bd32cbe73a98c7f6766f8a527e  # release/v1
        with:
          attestations: true
          skip-existing: true

      - name: Upload signed artifacts to the GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: gh release upload "${RELEASE_TAG}" dist/* sigstore-bundles/* --clobber
